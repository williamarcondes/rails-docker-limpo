Se já possui o Ruby instalado basta executar:
```
irb
```

Se preferir usar o docker
```
docker run --rm -ti ruby
```

Se preferir customizar seu container
```
docker run -it --rm meu-ruby:latest irb
```